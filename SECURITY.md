# Security Policy

## 报告安全漏洞

请通过私有渠道报告安全漏洞（不要公开 issue）：

- **邮箱**：由部署方在部署前配置的真实私有地址（仓库内仅保留占位符 `security@helix.example`，见下「部署前配置」）。
- **主题**：以 `[SECURITY]` 开头，描述影响与复现步骤。

## 部署前配置（M0 / SEC-007 闭环）

`security@helix.example` 是占位符。生产或类生产部署在满足以下检查清单之前视为**未通过安全报告闭环**，不得宣称生产就绪：

1. 用真实的私有接收渠道替换占位符邮箱（并同步到遥测/告警路由）。
2. 执行一封测试报告，走完「确认 → 分级 → 修复/缓解 → 关闭」完整流程，验证 SLA 计时起点与告警可达。
3. 配置 `DSR_EXPORT_SECRET`（数据主体导出 fail-closed 要求），并在 staging 演练一次导出/删除审批链。
4. 将本次演练证据（日期、负责人、结论）记录到部署发布说明；任何未批准的 P0/P1 例外都必须有 owner、≤14 天到期和补偿控制。

上述任一项未完成即视为安全报告闭环未交付，对应的能力不得在部署文档中被描述为生产就绪。

## 处置 SLA

| 严重级别 | 确认 | 修复目标 |
|---------|------|---------|
| Critical（远程 RCE / 未授权数据泄露） | 24 小时内 | 7 天内发布修复 |
| High（认证绕过 / 越权） | 48 小时内 | 14 天内 |
| Medium（信息泄露 / 拒绝服务） | 5 个工作日内 | 30 天内 |
| Low | 10 个工作日内 | 随下一个次版本 |

## 披露政策

- 修复发布后公开披露（含 CVE 编号如有）。
- 报告者署名（如同意）。
- 90 天协调披露窗口：发现后先通知维护方，90 天后可公开。

## 受支持版本

- `1.x` 当前次版本获得安全修复；上一个次版本仅接收 Critical/High 修复，窗口 90 天。
- 安全修复会以补丁版本发布并记录于 `CHANGELOG.md`。

## 报告检查清单

- 影响的产品与版本（`GET /api/me` 或 `APP_VERSION`）。
- 复现步骤（最小样例）。
- 影响评估（数据泄露范围、可利用条件）。
- 如已知缓解措施一并提供。

## 威胁模型

详见 `docs/SECURITY_MODEL.md`（STRIDE 分析、信任边界、控制矩阵）。

安全治理 gate（SEC-008）：每个 release 必须提交 threat-model delta（新增入口/
资产/信任边界、关闭/新增风险、控制与验证证据，具名 owner/审批人），季度桌面
演练（报告接收/依赖漏洞/密钥泄露/跨租户告警）不得过期 90 天以上；
`scripts/threat_model_gate.py` 校验缺失 delta、未命名/placeholder owner、
未来日期与过期演练，违规使 release/readiness gate 失败。台账与用法见
`docs/OPERATIONS.md`（Security Governance Gate 小节）、设计见
`docs/adr/0012-security-governance.md`。
